# IRUS V4 Build Requirements
# Core application dependencies
pyinstaller>=5.0
pillow>=9.0.0
opencv-python>=4.5.0
mss>=6.1.0
pyautogui>=0.9.53
keyboard>=0.13.5
numpy>=1.21.0
psutil>=5.8.0

# Optional compression
# upx (external tool - install separately if desired)

# Note: If you get import errors, you may need to install additional packages:
# pip install pynput (for advanced keyboard handling)
# pip install cryptography (for additional security features)